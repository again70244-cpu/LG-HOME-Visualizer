# CSP 不放在這裡，放在 index.html 的 <meta> —— 原因是 _headers 的規則會一併
# 套用到 sw.js，而多個 CSP 標頭同時存在時是取交集：頁面需要的 connect-src 'none'
# 和 Service Worker 需要的 connect-src 'self' 交集後等於什麼都不准，
# 結果就是離線快取永遠建不起來。
# meta CSP 只作用於文件本身，Service Worker 不受影響，兩邊各自拿到正確的限制。
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: no-referrer
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), interest-cohort=()

/sw.js
  Cache-Control: no-cache

/fonts/*
  Cache-Control: public, max-age=31536000, immutable
/icons/*
  Cache-Control: public, max-age=31536000, immutable
